<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!--
        SECURITY NOTE: This app requests CAMERA only for the local QR scanner.
        Specifically, android.permission.INTERNET is NOT declared here.
        Android enforces network access at install time based on this manifest -
        an app with no INTERNET permission is physically unable to open a socket,
        make an HTTP request, or send any data anywhere. This is enforced by the
        OS kernel/sandbox, not by app logic, so it cannot be bypassed by a bug
        in this app's own code.
    -->

    <application
        android:allowBackup="false"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false"
        android:theme="@style/AppTheme">

        <activity
            android:name=".SettingsActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ThemeSettingsActivity"
            android:exported="false"
            android:label="@string/settings_title" />

        <activity
            android:name=".EncryptActivity"
            android:exported="false"
            android:label="@string/encrypt_title" />

        <activity
            android:name=".ContactPairingActivity"
            android:exported="false"
            android:label="@string/pairing_title" />

        <activity
            android:name=".AttachmentPickerActivity"
            android:exported="false"
            android:theme="@style/AppTheme" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".FileCryptoActivity"
            android:exported="false"
            android:label="@string/file_crypto_title" />

        <!-- External receive entry point for SecureKeyboard encrypted files.
             Only this small handler is exported; the keyboard and crypto screens remain private. -->
        <activity
            android:name=".SecureFileReceiveActivity"
            android:exported="true"
            android:label="@string/secure_file_receive_title"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" android:mimeType="application/octet-stream" />
                <data android:scheme="content" android:pathPattern=".*\\.skf" />
                <data android:scheme="file" android:pathPattern=".*\\.skf" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/octet-stream" />
            </intent-filter>
        </activity>

        <activity
            android:name=".AboutActivity"
            android:exported="false"
            android:label="@string/about_title" />

        <!-- App-owned QR scanner: stable theme + FLAG_SECURE across OEMs. -->
        <activity
            android:name=".SecureCaptureActivity"
            android:exported="false"
            android:screenOrientation="fullSensor"
            android:stateNotNeeded="true"
            android:theme="@style/ScannerTheme"
            android:windowSoftInputMode="stateAlwaysHidden" />

        <service
            android:name=".SecureInputMethodService"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:exported="true"
            android:label="@string/keyboard_name">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

    </application>
</manifest>
